server:
  port: 8080
  servlet:
    context-path: /e-learning-portal

# spring
spring:
  application:
    name: '@project.artifactId@'
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:google:mysql://e-learning-back-end:europe-west2:e-learning-instance?serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
#  jackson:
#    serialization:
#      INDENT_OUTPUT: true

